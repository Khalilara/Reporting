import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.web.servlet.config.annotation.CorsRegistry;
import org.springframework.web.servlet.config.annotation.WebMvcConfigurer;

@Configuration
public class CorsConfig {

    @Bean
    public WebMvcConfigurer corsConfigurer() {
        return new WebMvcConfigurer() {
            @Override
            public void addCorsMappings(CorsRegistry registry) {
                // Autoriser TOUTES les routes
                registry.addMapping("/**")
                        .allowedOriginPatterns("*")  // ✅ Toutes les origines
                        .allowedMethods("*")         // ✅ Toutes les méthodes HTTP
                        .allowedHeaders("*")         // ✅ Tous les headers
                        .allowCredentials(true)      // ✅ Autoriser les credentials
                        .maxAge(3600);               // ✅ Cache des preflight
            }
        };
    }
}